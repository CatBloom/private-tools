# private-tools エージェントガイド

## プロジェクトの目的

- 小さな GitHub Issue 単位で成長できる、非公開・モバイルファーストの個人用ユーティリティ Web アプリケーションを構築する。
- 初期の本番アーキテクチャは、可能な限りシンプルかつ安全にし、無料枠内に収める。
- `requirements.html` を、現在のプロダクト要件および運用要件の正とする。

## 確定済みのアーキテクチャ

- アプリケーション: Hono SSR、React、Vite、TypeScript。
- ホスティング: Vercel。本番ブランチは `main`。
- アクセス制御: Cloudflare Access。アプリケーションでアカウント登録は実装しない。
- 永続ストレージ: Cloudflare Workers KV を REST API 経由で使用する（開発時は KV 環境変数が無ければローカルフォールバック）。
- ドメイン: 将来的に `catbloom.net` のサブドメインを使用する。
- UI: モバイルファーストで、デスクトップでも利用可能にする。
- 実行環境: Node.js 24 / pnpm。

## 責務の境界

- 実装は Claude Code（オーケストレーター）が、ロール別サブエージェント（PM / Engineer / Reviewer / QA Engineer）を使って行う。
- Codex はレビュー担当。アプリケーションコードの作成・変更は行わない。
- ユーザーは、学習のために外部アカウントを作成し、秘密情報を入力し、クラウド管理画面と DNS を変更し、本番デプロイ操作を行う。
- API トークン、OAuth シークレット、パスワード、リカバリーコード、実際の金融データを、ソースファイル、フィクスチャ、ログ、Issue、チャット出力に絶対に置かない。
- 現在のユーザーリクエストで明示的に許可されない限り、push、マージ、デプロイ、GitHub またはクラウド設定の変更など、外部への書き込みを行わない。

## サブエージェントのワークフロー

- オーケストレーター（セッション親）が唯一の指揮点であり、サブエージェントの起動・進捗管理・成果物の受け渡しをすべて担う。
- ロールは PM（タスク分解・体制案）／ Engineer（実装）／ Reviewer（Claude レビュー）／ QA Engineer（テスト実行・ブラウザ実測）。規模の大きいチケットは、まず PM にタスク分解と体制案（必要ロール・人数・並列可否）を出させる。
- やり取りは星型が基本で、サブエージェント同士は直接対話させない。成果物は必ずオーケストレーター経由で渡す（レビュー指摘は鵜呑みにせず、妥当性を判断してから Engineer に渡す）。機械的な往復（承認済み指摘の修正→再確認など）に限り、範囲と往復回数を指定して直接やり取りを許可してよい。
- 品質ゲートは Engineer → Reviewer（Claude レビュー）→ QA（テスト実行・ブラウザ実測）の順。規模に応じて省略できるが、削除・移行・破壊的変更を含む場合は省略しない。
- 並列で作業する Engineer には worktree isolation を使い、同時に同じ作業ツリーを編集させない。
- 各サブエージェントには、範囲が限定された目標、関連する制約、明確な停止条件を与える。
- PR 作成後は Codex レビューを実行し、「Codex Review」全文と「Claude の検証結果」の 2 コメントを PR に投稿してからマージする。レビュー範囲は、個別 PR は変更箇所だけ、リリースブランチ（複数 PR のまとめ）は全体、指摘修正後の再レビューは指摘部分だけとする。

## エンジニアリングのワークフロー

- 無関係な変更およびユーザー作成の変更を保全する。単純化のために上書きや削除をしない。
- パッケージマネージャーは pnpm を使用する。
- 依存関係は、複雑さまたはリスクを実質的に減らす場合にのみ追加する。本番依存関係は引き継ぎ時に説明する。
- サーバー専用の認証情報および Cloudflare API 呼び出しをブラウザバンドルに含めない。
- 振る舞いを変更した場合は、テストを追加または更新する。
- コードを引き渡す前に、存在する該当スクリプト（lint、typecheck、test、build）を実行する。
- 実行したチェックと、実行できなかったチェックを正確に報告する。

UI、SSR、静的資産、または開発時 CSP を変更した場合は、lint/typecheck/test/build だけで完了扱いにしない。開発サーバーを起動し、`/`、CSS、client module を HTTP 取得して確認する。ブラウザまたはブラウザー相当でコンソールエラー、hydration エラー、CSP 違反がないことも確認する（実測は QA Engineer が担当する）。実行環境の障害により実ファイルまたは実行結果を確認できない場合は「問題なし」とせず「未検証」と報告し、完了判定を保留する。

## Git とデリバリー

- 作業は必ず最新の `main` からブランチを切って始める。コミットは 1 つの関心事に限定する。
- Pull Request のタイトルと本文は日本語で記載する。本文は少なくとも `## 概要` と `## 説明` の見出しで構成する。
- `main` にマージする前に、Pull Request は自動チェックを通過しなければならない。
- Vercel 接続後は、`main` へのマージが本番デプロイのトリガーとなる。
- 新しいツール・機能は、実装前に GitHub Issue で定義しなければならない。

## ローカルタスク管理

- ローカルの進捗メモとチェックリストは `task/` 配下に置く。このディレクトリは Git で無視され、その内容をコミットしてはならない。
- ユーザーからエージェントのミスを指摘された場合は、`task/MISTAKES.md` に事実、原因、再発防止策を簡潔に追記する。秘密情報や不要な会話ログは記録しない。
- 作業の次の段階や進捗を案内する前に、`task/PROJECT_CHECKLIST.md` と必要に応じて `task/MISTAKES.md` を確認する。
- `requirements.html` を正とする。ローカルチェックリストは進捗を要約するものであり、要件を置き換えたり再定義したりしない。
- チェックリストを更新する際は、リポジトリまたは設定の根拠から確認済みの作業と、ユーザーから完了報告を受けた外部作業を区別する。
- 根拠が得られない限り、ユーザー管理の外部設定を確認済みとして扱わない。ユーザー報告による完了は明示的に記録する。
